#pragma once
/*
#include <iostream>
#include "Rect.hpp"
enum MyEnum
{
	BLOC_UNIT_LIST,
	BLOC_UNIT,
	BLOC_TIMER,
	BLOC_SCORE,
	nb_bloc
};

struct HudBlock
{
	HudBlock(const Rect &nBackground, const Rect &nRectMessage, const std::string &nMessage) :
		background(nBackground),
		rectMessage(nRectMessage),
		message(nMessage) {}
	Rect				background;
	Rect				rectMessage;
	std::string			message;
};
*/
